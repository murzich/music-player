const identifier = '@@Playlist/';

export const ADD_TRACK = `${identifier}ADD_TRACK`;
export const CLEAR_PLAYLIST = `${identifier}CLEAR_PLAYLIST`;
export const REMOVE_TRACK = `${identifier}REMOVE_TRACK`;
